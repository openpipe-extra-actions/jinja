_libraries:
    - .       # Test from the local version

start:

    - iterate:
        - { name: João, age:  80 }
        - { name: Bob, age: 40 }
    - send to segment:
        - single_item
        - multi_item

single_item:
    - transform using jinja:
        template: name.txt.jinja
        single_item: True
    - print:

multi_item:
    - transform using jinja:
        template: multi_name.txt.jinja
        single_item: False
    - print:
